import { StyleSheet } from "react-native";

export default StyleSheet.create({
  previousButton: {
    backgroundColor: "#f1f1f1",
    color: "black",
    marginRight: 10
  },
  nextButton: {
    fontSize: 20,
    textAlign: "center"
  }
});
